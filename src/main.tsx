import React from "react";
import ReactDOM from "react-dom/client";
import MechBoard from "./MechBoard";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MechBoard />
  </React.StrictMode>
);
